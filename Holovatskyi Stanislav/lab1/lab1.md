
Міністерство освіти і науки України
ЛЬВІВСЬКИЙ НАЦІОНАЛЬНИЙ УНІВЕРСИТЕТ ВЕТЕРИНАРНОЇ
МЕДИЦИНИ ТА БІОТЕХНОЛОГІЙ ІМЕНІ С.З. ҐЖИЦЬКОГО
Звіт
про виконання лабораторної роботи №1 з дисципліни "об'єктно орієнтовне
програмування" на тему "Вступне заняття. Налаштування середовища розробки Python 3
і запуск програм"
Виконав: студент групи Іт-21 Головацький Станіслав
Прийняв: ст. викладач Заплатинський Н.Б.
Львів 2026
Мета: ознайомитися інструментарієм розробки веб-додатків.
Хід роботи
1. Встановила Python 3 та розібралася в його роботі
2. Написала базову програму та проаналізувала її роботу:
3. Навчилася використовувати систему Git для контролю версій програмного
забезпечення.
name = input("Як вас звати?: ")
print(f"Привіт, {name}!")
• Команда name = працює як посилання на те, про що йде після дорівнює
• Команда input("Як вас звати?: ") зчитує дані з клавіатури після введеного в
лапках тексту
• Команда print виводить результат на екран
• Команда f дозволяє вставляти змінні в текст, в нашому випадку це є введене з
клавіатури ім'я
• Команда {name} те саме посилання на відповідь у першому рядку
Відповіді на запитання самоконтролю
1. Як встановити середовище розробки Python? Як запустити виконання програми?
Для встановлення Python 3 необхідно завантажити інсталятор з офіційного сайту
python.org та запустити його. Під час встановлення слід відмітити опцію "Add Python to
PATH". Для запуску програми у Windows використовують команду python
назва_файлу.py у командному рядку, а в Unix-подібних системах — python3
назва_файлу.py. Також програму можна запустити з середовища розробки (IDE),
наприклад PyCharm або VS Code.
2. Які основні відмінності в роботі під ОС Windows та Unix-подібними системами?
У Windows команда запуску Python — python, тоді як у Unix-подібних системах (Linux,
macOS) використовується python3, щоб відрізнити від Python 2. Роздільник шляхів у
Windows — зворотна коса риска (\), а в Unix — пряма (/). Кінці рядків у файлах теж
відрізняються: Windows використовує CRLF (\r\n), а Unix — LF (\n). Крім того, у Unixсистемах Python зазвичай вже встановлений за замовчуванням.
3. Поясніть структуру найпростішої програми на Python 3.
Найпростіша програма на Python 3 складається з послідовних інструкцій, які
виконуються зверху вниз. Наприклад:
name = input("Як вас звати?: ")
print(f"Привіт, {name}!")
Перший рядок — зчитування даних від користувача за допомогою функції input() і
збереження результату у змінній name. Другий рядок — виведення тексту на екран за
допомогою функції print(). Символ f перед рядком дозволяє підставляти значення
змінних у текст за допомогою фігурних дужок {name}.
