## Міністерство освіти і науки України

## ЛЬВІВСЬКИЙ НАЦІОНАЛЬНИЙ УНІВЕРСИТЕТ ВЕТЕРИНАРНОЇ МЕДИЦИНИ ТА БІОТЕХНОЛОГІЙ ІМЕНІ С.З. ҐЖИЦЬКОГО

# Звіт
про виконання лабораторної роботи №3 з дисциплини "Об'єктно-орієнтоване програмування" 
на тему "Основи структурного програмування в Python 3"

Виконав: студент групи ІТ-21 Запорожцев Матвій
Прийняв: Ст. викладач Заплатинський Назар Богданович

## Львів 2026

Мета: Ознайомлення з основними прийомами структурного програмування у Python 3. 

## Хід роботи

1. Засвоїти суть структурного програмування. 
Структурне програмування це підхід до програмування, який забезпечує чітку логіку та читабельність коду завдяки відмові від безладних переходів (GOTO) на користь логічних блоків та процедур.

2. Навчитися використовувати умовні оператори.
Умовні оператори це оператори порівняння:

 ==   Дорівнює	                x == y	
 !=   Не дорівнює	            x != y	
 >    Більше ніж	            x > y	
 <	  Менше ніж	                x < y	
 >=   Більше ніж або дорівнює	x >= y	
 <=   Менше ніж або дорівнює    x <= y

3. Познайомитися з операторами булевої логіки в Python 3. 
+

4. Навчитися проектувати складні умови.  
```
x = 14
if 10 < x < 20:
    print("Число знаходиться в діапазоні")
```

5. Опанувати методи роботи з циклами. Навчитися визначати доцільність застосування різних типів циклів залежно від поставленої задачі. 
```
x = 0
while x < 4:
    for i in range(6):
        x += 1
        print(x)
        if x == 5:
            break
    break
```

6. Розв’язати приклади згідно виданих завдань.
Дано 4 числа a, b, c, d. Знайти max{min(a, b), min(c, d)}:
```
a = 1
b = 2
c = 3
d = 4
x = max(min(a, b), min(c, d))
print(x)
```

Для даного x обчислити значення функції: 
F(x) = {9 ,         якщо x <= -3
       {1/(x^2+1) , якщо x > -3
```
def func(x):
    if x <= -3:
        return 9
    elif x > -3:
        return 1/((x**2)+1)
    else:
        return "chomu"
        
print(func(-4))

# > 9
```

## Висновок 
У ході виконання лабораторної роботи я ознайомився з основними прийомами структурного програмування у Python 3. 
